/**
 * Types du contrat d'API — ré-exportés depuis le fichier généré par le backend
 * (`npm run sync:types` côté SmartAttendance-Backend). Ne rien redéclarer ici.
 */
export type {
  ApiErrorPayload as ApiError,
  ApiUser as User,
  FaceCheckInPayload,
  FaceCheckInResponse,
  LoginResponse,
  UserRole,
} from './shared-types.generated';
